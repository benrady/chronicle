#!/usr/bin/env jruby --headless

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

require 'chronicle'

if ARGV.length < 2
  puts "Not enough arguments"
  puts ""
  puts "Usage: chron-prepare sheet_info_file scenario_name [output_dir]"
  exit 1
end
Chronicle.finish(*ARGV)
